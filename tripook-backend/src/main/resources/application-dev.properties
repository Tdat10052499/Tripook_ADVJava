# Development Profile Configuration
# This file is loaded when spring.profiles.active=dev

# Development Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/tripook_dev_db
spring.datasource.username=tripook_dev_user
spring.datasource.password=tripook_dev_password

# Development JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Development Security Configuration (disable for development)
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN