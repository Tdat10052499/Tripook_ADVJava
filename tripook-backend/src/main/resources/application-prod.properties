# Production Profile Configuration
# This file is loaded when spring.profiles.active=prod

# Production Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/tripook_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Tdat100524}

# Production JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Production Logging Configuration
logging.level.com.tripook=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# Production Server Configuration
server.error.include-message=never
server.error.include-binding-errors=never